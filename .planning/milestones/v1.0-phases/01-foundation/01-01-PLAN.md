---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - .gitignore
  - storage.js
  - tests/storage.test.js
autonomous: true
requirements:
  - FOUNDATION-SCAFFOLDING
  - FOUNDATION-STORAGE
must_haves:
  truths:
    - "npm install completes without errors and node_modules/ exists"
    - ".env.example contains TELEGRAM_TOKEN and ANTHROPIC_API_KEY"
    - ".env is gitignored"
    - "data/ directory is gitignored"
    - "readExpenses returns an empty array for a nonexistent user file"
    - "appendExpense writes an expense to data/{userId}.json and it persists on disk"
    - "Two concurrent appendExpense calls for the same user both appear in the final file"
    - "popExpense removes and returns the last expense from a user's file"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "node-telegram-bot-api"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "TELEGRAM_TOKEN"
    - path: ".gitignore"
      provides: "Git ignore rules"
      contains: "data/"
    - path: "storage.js"
      provides: "Concurrency-safe per-user JSON file storage"
      exports: ["readExpenses", "appendExpense", "popExpense"]
    - path: "tests/storage.test.js"
      provides: "Unit tests for storage module including concurrency"
  key_links:
    - from: "storage.js"
      to: "async-mutex"
      via: "require('async-mutex')"
      pattern: "require\\('async-mutex'\\)"
    - from: "storage.js"
      to: "write-file-atomic"
      via: "require('write-file-atomic')"
      pattern: "require\\('write-file-atomic'\\)"
    - from: "storage.js"
      to: "data/{userId}.json"
      via: "path.join(DATA_DIR, userId + '.json')"
      pattern: "path\\.join.*DATA_DIR"
---

<objective>
Set up the project scaffolding (package.json, .env.example, .gitignore) and implement the concurrency-safe storage module with full test coverage.

Purpose: Establish the runnable Node.js project and the data persistence layer that all subsequent phases write into. The storage module is the most architecturally critical piece -- it must handle concurrent writes safely.
Output: Installable project with tested storage.js module.
</objective>

<execution_context>
@/workspaces/mixxy/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/mixxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding and storage module with tests</name>
  <files>package.json, .env.example, .gitignore, storage.js, tests/storage.test.js</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - readExpenses(unknownUserId) returns []
    - appendExpense(userId, { amount: 35000, category: 'makan', description: 'nasi goreng', timestamp: '2026-03-17T10:00:00.000Z' }) writes to data/{userId}.json
    - readExpenses(userId) after appendExpense returns array containing the expense
    - appendExpense called twice sequentially results in array of length 2
    - Two concurrent appendExpense calls for the SAME userId (Promise.all) both appear in the final file (array length = 2)
    - Two concurrent appendExpense calls for DIFFERENT userIds do not interfere with each other
    - popExpense(userId) removes and returns the last expense; readExpenses then returns one fewer
    - popExpense on empty user returns null
    - Expense object shape: { amount: number, category: string, description: string, timestamp: string }
  </behavior>
  <action>
    **Step 1: Create package.json**

    Run `npm init -y` in the project root, then update the generated file:
    - Set `"name": "mixxy"`
    - Set `"version": "1.0.0"`
    - Set `"description": "Telegram expense tracker bot for Indonesian users"`
    - Set `"main": "index.js"`
    - Add `"scripts": { "test": "node --test tests/", "start": "node index.js" }`
    - Set `"license": "MIT"`

    Then install dependencies:
    ```
    npm install node-telegram-bot-api@0.67.0 dotenv@17.3.1 async-mutex@0.5.0 write-file-atomic@7.0.1
    ```

    NOTE on write-file-atomic: version 7.x is ESM-only. If `require('write-file-atomic')` fails at runtime, fall back to version 5.x (`npm install write-file-atomic@5`) which is CommonJS. Alternatively, use dynamic `import()` inside the async functions. Check this during implementation.

    **Step 2: Create .env.example**

    Content (exactly):
    ```
    # Telegram bot token from @BotFather
    TELEGRAM_TOKEN=your_telegram_bot_token_here

    # Anthropic API key (used in Phase 2)
    ANTHROPIC_API_KEY=your_anthropic_api_key_here
    ```

    **Step 3: Create .gitignore**

    Content (exactly):
    ```
    node_modules/
    .env
    data/
    *.log
    ```

    **Step 4: Create tests/storage.test.js (RED phase)**

    Use `node:test` and `node:assert` (built-in, zero deps). Structure:

    ```javascript
    const { describe, it, beforeEach, afterEach } = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('fs').promises;
    const path = require('path');
    ```

    Before each test: set a temp data dir (e.g. `tests/tmp-data-{random}`).
    After each test: remove the temp dir recursively.

    Override `DATA_DIR` in storage module. To make this testable, storage.js should either:
    (a) Export a `_setDataDir(dir)` function for testing, OR
    (b) Accept `dataDir` as a parameter to exported functions, OR
    (c) Use an environment variable `DATA_DIR` with a default.

    Recommended: option (c) -- `const DATA_DIR = process.env.DATA_DIR || path.join(__dirname, 'data');`

    Tests to write (these MUST fail initially since storage.js doesn't exist yet):
    1. `readExpenses returns empty array for nonexistent user` -- call readExpenses('user999'), assert deepEqual []
    2. `appendExpense creates file and stores expense` -- append one expense, readExpenses, assert length 1, assert expense.amount === 35000
    3. `appendExpense appends to existing expenses` -- append two expenses sequentially, assert length 2
    4. `concurrent appendExpense for same user preserves both` -- Promise.all([appendExpense(userId, exp1), appendExpense(userId, exp2)]), readExpenses, assert length === 2
    5. `concurrent appendExpense for different users does not interfere` -- Promise.all for user1 and user2, read each, assert each has length 1
    6. `popExpense removes last expense` -- append 2 expenses, pop 1, assert readExpenses length === 1, assert returned expense matches last appended
    7. `popExpense on empty user returns null` -- pop from nonexistent user, assert result === null
    8. `expense has correct shape` -- append expense, read it back, assert it has amount, category, description, timestamp keys

    **Step 5: Run tests (confirm RED)**

    Run `node --test tests/storage.test.js` -- all tests must fail (storage.js doesn't exist).

    **Step 6: Create storage.js (GREEN phase)**

    Implement exactly as specified in RESEARCH.md Pattern 2, with these specifics:
    - `'use strict';` at top
    - `const DATA_DIR = process.env.DATA_DIR || path.join(__dirname, 'data');`
    - `const { Mutex } = require('async-mutex');`
    - For write-file-atomic: try `require('write-file-atomic')` first. If it fails (ESM-only in v7), use `fs.writeFile` with a logged warning OR install v5 instead. The critical safety comes from the mutex; atomic write is defence-in-depth.
    - `getMutex(userId)` -- Map-based, coerces userId to String
    - `readExpenses(userId)` -- reads file, parses JSON, returns [] on ENOENT
    - `appendExpense(userId, expense)` -- mkdir recursive, mutex.runExclusive, read-push-write pattern. Normalizes expense to `{ amount, category, description, timestamp }` shape.
    - `popExpense(userId)` -- mutex.runExclusive, read-pop-write, returns removed expense or null if empty
    - `module.exports = { readExpenses, appendExpense, popExpense };`

    **Step 7: Run tests (confirm GREEN)**

    Run `node --test tests/storage.test.js` -- all tests must pass.
  </action>
  <verify>
    <automated>node --test tests/storage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"node-telegram-bot-api"` in dependencies
    - package.json contains `"async-mutex"` in dependencies
    - package.json contains `"dotenv"` in dependencies
    - package.json contains `"scripts"` with `"test": "node --test tests/"`
    - .env.example contains `TELEGRAM_TOKEN=your_telegram_bot_token_here`
    - .env.example contains `ANTHROPIC_API_KEY=your_anthropic_api_key_here`
    - .gitignore contains `node_modules/`
    - .gitignore contains `.env`
    - .gitignore contains `data/`
    - storage.js contains `require('async-mutex')`
    - storage.js contains `module.exports`
    - storage.js contains `readExpenses`
    - storage.js contains `appendExpense`
    - storage.js contains `popExpense`
    - storage.js contains `getMutex`
    - storage.js contains `runExclusive`
    - storage.js contains `DATA_DIR`
    - tests/storage.test.js contains `require('node:test')`
    - tests/storage.test.js contains `require('node:assert`
    - tests/storage.test.js contains `Promise.all` (concurrent test)
    - `node --test tests/storage.test.js` exits with code 0
    - node_modules/ directory exists
  </acceptance_criteria>
  <done>
    npm install succeeds. storage.js exports readExpenses, appendExpense, popExpense. All 8 storage tests pass including concurrent write safety for same user. .env.example and .gitignore exist with correct content.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/storage.test.js` passes all tests (including concurrency test)
- `cat .env.example` shows both TELEGRAM_TOKEN and ANTHROPIC_API_KEY
- `cat .gitignore` shows node_modules/, .env, data/
- `node -e "const s = require('./storage.js'); console.log(Object.keys(s))"` prints readExpenses, appendExpense, popExpense
</verification>

<success_criteria>
- Project is installable (npm install completes)
- Storage module handles concurrent writes from same user without data loss
- All storage tests green
- Environment template and gitignore in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
